{% extends 'base.html.twig' %}

{% block title %}ShopContent{% endblock %}

{% block body %}
    <h1>ShopContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop_content.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ shop_content.quantity }}</td>
            </tr>
            <tr>
                <th>Create_at</th>
                <td>{{ shop_content.createAt ? shop_content.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shop_content_index') }}">back to list</a>

    <a href="{{ path('shop_content_edit', {'id': shop_content.id}) }}">edit</a>

    {{ include('shop_content/_delete_form.html.twig') }}
{% endblock %}
